spring:
  application:
    name: movies-payment-service
  data:
    mongodb:
      uri: mongodb://localhost:10122/db
      database: db

server:
  port: 0

eureka:
  client:
    service-url:
      defaultZone: "http://localhost:8080/eureka"

axon:
  serializer:
    general: jackson
    events: jackson
    messages: jackson
